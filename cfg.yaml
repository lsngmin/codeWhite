task: detect
mode: train

model: /home/codeWhite/ultralytics/started/best.pt
data: /home/codeWhite/ultralytics/started/tld_2024.yaml

project: /home/codeWhite/ultralytics/started/trainModel/
name: tld_detection2

epochs: 50
patience: 3
batch: 22
imgsz: 1280
device: 0,1
workers: 48
cache: disk
pretrained: True
val: True

optimizer: AdamW

cos_lr: False
close_mosaic: 0


conf: 0.69
iou: 0.65


lr0: 0.001
lrf: 0.0085


momentum: 0.937
weight_decay: 0.0005  # optimizer weight decay 5e-4


warmup_epochs: 10.0  # warmup epochs (fractions ok)
warmup_momentum: 0.9  # warmup initial momentum
warmup_bias_lr: 0.1  # warmup initial bias lr


box: 8.0 # box loss gain
cls: 1.3
dfl: 1.5  # dfl loss gain


label_smoothing: 0.06  # label smoothing (fraction)
nbs: 64  # nominal batch size
hsv_h: 0.03  # image HSV-Hue augmentation (fraction)
hsv_s: 0.7  # image HSV-Saturation augmentation (fraction)
hsv_v: 0.5
translate: 0.3 # image translation (+/- fraction)
scale: 0.8 # image scale (+/- gain)
shear: 0.1  # image shear (+/- deg)
perspective: 0.1 # image perspective (+/- fraction), range 0-0.001
#flipud: 0.0  # image flip up-down (probability)
#fliplr: 0.5  # image flip left-right (probability)
mosaic: 0.8  # image mosaic (probability)
mixup: 0.38  # image mixup (probability)
copy_paste: 0.2  # segment copy-paste (probability)


augment: True
seed: 0
deterministic: True
rect: False


max_det: 15
half: False
save: True
save_period: -1
exist_ok: True
verbose: True
single_cls: False
resume: False
split: val
save_json: False
format: torchscript
workspace: 4
tracker: botsort.yaml  # tracker type, ['botsort.yaml', 'bytetrack.yaml']
